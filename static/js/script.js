window.onload = function () {
    const videoElement = document.getElementById('videoFeed');
    
    setInterval(() => {
       
        console.log('Sending gesture data to backend...');
    }, 1000); // Update every second (adjust based on need)
};
